namespace MetroShip.Repository.Neo4j;

public interface INeo4JRepository
{
    // Task CreateMetroLineAsync(int id, string name, double totalLength, int totalStations);
    // Task CreateRouteAsync(int id, int fromStationId, int toStationId, double lengthKm, string direction);
    // Task CreateStationAsync(int id, string name, double latitude, double longitude);
}